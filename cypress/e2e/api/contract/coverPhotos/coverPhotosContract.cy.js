

// describe('Contrato no módulo Cover Photos', () => {
//     context('Validando contratos de sucesso', function () {
//         it.only('deve validar status e schema', () => {
//             cy.request('/api/v1/CoverPhotos').then((response) => {
//                 expect(response.status).to.eq(200)
//                 cy.validateSchema(getCoverPhotos, response.body)
//             })
//         })
//     })
